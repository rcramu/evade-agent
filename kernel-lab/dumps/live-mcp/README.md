# Live Phase 2 MCP

`mcp-live.jsonl` is TLS 1.3 audit from the lab MCP (`document.search` /
`document.read` only). No tokens are stored.

`observer.jsonl` is the container observer snapshot of the `evade-phase2`
compose project.

Recapture: `./kernel-lab/compose/run.sh`
